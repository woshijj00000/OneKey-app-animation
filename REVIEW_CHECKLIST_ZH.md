# OneKey App Animation · 强制 Review 流程

每个视频在交付前必须完成两道 Review。任何适用项目出现 `FAIL`，都必须先修正并重新检查，不能直接交付。确实不适用的项目标记为 `N/A`，并写明原因。

## Review Gate A：正式渲染前

### A1. 代码与素材

- [ ] 已读取完整截图、交互顺序和参考视频。
- [ ] 截图状态与交互步骤一一对应，没有漏页或错页。
- [ ] 原始 UI 未被重绘、拉伸或改变样式。
- [ ] 已运行 `npm run lint`，且无错误。

### A2. 关键帧取样

至少检查以下渲染帧，而不是只看代码：

- [ ] 每个页面完全进入后的帧。
- [ ] 每个长页面滚动开始、中间和结束的帧。
- [ ] 每个气泡完整显示时的帧。
- [ ] 每个气泡消失后的帧。
- [ ] 每个点击按下和回弹时的帧。
- [ ] 每次页面转场中间的帧。
- [ ] 每个包含隐私信息的页面及其衍生状态。
- [ ] Logo 出现和完全显示时的帧。

### A3. 逐项视觉规范

- [ ] 画布为 1080 × 1080、60fps。
- [ ] 背景固定使用 `public/source/onekey-gradient-39db00-00c9a5.png`，其颜色从顶部 `#39DB00` 向底部渐变至 `#00C9A5`；没有使用近似 CSS 渐变或其他底图替代。
- [ ] UI 固定为 `left: 144px`、`top: 160px`、`width: 792px`，水平居中；顶部圆角固定为 100px，没有真实手机外框。
- [ ] 没有多余的介绍文字，原始 UI 样式没有被修改。
- [ ] 长页面从顶部连续滚动，按钮完整出现后才点击。
- [ ] 点击坐标准确落在真实控件上。
- [ ] 点击圆点在点击位置原地出现，没有从底部或边缘移动进入。
- [ ] 点击为浅绿外圆和亮绿内圆的双层错峰 Q 弹动画。
- [ ] 点击后没有放射线、扩散圆环、外溢光圈或闪光。
- [ ] 需要说明的点击点有气泡；`Export`、`Copy` 等明确指定的按钮没有气泡。
- [ ] 气泡底色统一为 `#031F00`，文字统一为 `#DBFFD7`；圆角、小尖角指向目标，且不遮挡按钮。
- [ ] 气泡底帧必须无旧气泡、旧尾巴或旧阴影残留；淡入、停留、淡出期间不得出现双色重叠。
- [ ] 英文使用 Roobert SemiBold，中文使用 MiSans SemiBold。
- [ ] 气泡完整停留 0.8 秒，完全消失后等待 0.1 秒再点击。
- [ ] 点击发生时气泡已经完全消失。
- [ ] 点击后保留短促按压反馈，页面以约 0.4 秒平滑切换，没有静止停顿。
- [ ] 所有敏感信息都由 `#F1F1F3`、10px 圆角的不透明矩形完整覆盖。
- [ ] 隐私遮罩跟随页面滚动和转场，在 Toast、成功状态等衍生画面中没有闪现原文。
- [ ] 结尾固定使用 `public/brand/onekey-app-user-fixed.png`，位置与尺寸为 `left: 190px`、`top: 465px`、`width: 700px`、`height: 143px`。
- [ ] Logo 只做透明度动画，没有位移或缩放，且画面中没有底层旧 Logo 或重复 Logo。

Gate A 全部通过后，才进行完整 MP4 渲染。

## Review Gate B：正式渲染后

### B1. 成品技术检查

- [ ] MP4 文件存在并可正常打开。
- [ ] 分辨率为 1080 × 1080。
- [ ] 帧率为 60fps。
- [ ] 时长完整，开头和 Logo 结尾没有被截断。
- [ ] 视频包含音轨。
- [ ] 点击音效、背景音乐和 Logo 出现音效均正常，音量不过载。

### B2. 完整播放检查

- [ ] 从头到尾观看一次实际导出的 MP4。
- [ ] 没有黑帧、闪帧、卡帧或看起来像暂停的静止段落。
- [ ] 页面顺序和点击逻辑正确。
- [ ] 所有滚动、气泡、点击和转场衔接自然。
- [ ] 整段视频内没有敏感信息露出。
- [ ] Logo 结尾显示正确，出现音效同步。

## Review 结果模板

每次交付视频时附上以下结果：

```text
Review 结果：PASS / FAIL
Composition：<名称>
输出文件：<绝对路径>
规格：1080×1080 / 60fps
时长：<秒>
音轨：PASS / FAIL
Gate A：PASS / FAIL
Gate B：PASS / FAIL
N/A 项：<没有则写“无”>
修正记录：<列出 Review 中发现并修正的问题；没有则写“无”>
```

只有 `Review 结果：PASS` 的视频才算完成。
